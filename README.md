# OSFingerprinting
This scan will attempt to detect the operating system of the target using the OS detection feature, and will also attempt to determine the version of software running on the target using version detection.
